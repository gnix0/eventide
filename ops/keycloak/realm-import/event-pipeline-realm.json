{
  "realm": "event-pipeline",
  "enabled": true,
  "displayName": "Event Pipeline",
  "roles": {
    "realm": [
      { "name": "platform-admin" },
      { "name": "tenant-admin" },
      { "name": "pipeline-editor" },
      { "name": "pipeline-viewer" },
      { "name": "topic-editor" },
      { "name": "topic-viewer" },
      { "name": "replay-operator" }
    ]
  },
  "clients": [
    {
      "clientId": "event-pipeline-api",
      "name": "Event Pipeline API",
      "enabled": true,
      "publicClient": false,
      "serviceAccountsEnabled": true,
      "protocol": "openid-connect",
      "standardFlowEnabled": true,
      "directAccessGrantsEnabled": true,
      "redirectUris": ["http://localhost:3000/*"],
      "webOrigins": ["http://localhost:3000"],
      "protocolMappers": [
        {
          "name": "audience-event-pipeline-api",
          "protocol": "openid-connect",
          "protocolMapper": "oidc-audience-mapper",
          "consentRequired": false,
          "config": {
            "included.client.audience": "event-pipeline-api",
            "id.token.claim": "true",
            "access.token.claim": "true"
          }
        }
      ]
    },
    {
      "clientId": "event-pipeline-cli",
      "name": "Event Pipeline CLI",
      "enabled": true,
      "publicClient": true,
      "protocol": "openid-connect",
      "standardFlowEnabled": false,
      "directAccessGrantsEnabled": true
    }
  ]
}
